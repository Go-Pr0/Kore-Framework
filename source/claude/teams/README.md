# Claude Teams

This directory is Claude-only.

Use it for team-specific documentation or templates that belong to native Claude Code team workflows.

Runtime team state is still created by Claude Code itself under `~/.claude/teams/` during actual team runs. Codex and Gemini do not receive team runtime state.
