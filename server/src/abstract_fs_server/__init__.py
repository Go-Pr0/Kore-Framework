"""Semantic MCP server package for generalized repository search.

Exposes search-only MCP tools over stdio and delegates indexing to the
Abstract Representation Engine. The runtime uses one normalized generalized
protocol for arbitrary code, config, and documentation files.
"""
